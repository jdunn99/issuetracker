subscription SendIssue {
	sendIssue {
		name
		desc
		severity
		status
		id
		createdAt
		createdBy {
			email
			name
			id
		}
		comments {
			id
			createdAt
			postedBy {
				id
				name
			}
			comment
		}
	}
}
