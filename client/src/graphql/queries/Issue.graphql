query Issue($id: Float!) {
	issue(id: $id) {
		name
		desc
		severity
		status
		id
		createdAt
		comments {
			id
			createdAt
			postedBy {
				id
				name
			}
			comment
		}
		createdBy {
			email
			name
			id
		}
	}
}
